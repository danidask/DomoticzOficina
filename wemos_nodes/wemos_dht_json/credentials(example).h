// Fill and rename to credentials.h
// credentials.h is included in .gitignore, so you won't upload your password to github

// Existing WiFi network
const char* ssid     = "my_wifi";
const char* password = "my_password";
